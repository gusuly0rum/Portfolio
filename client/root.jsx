import App from 'app';
import React from 'react';
import { HashRouter } from 'react-router-dom';

const Root = () => {
  return (
    <HashRouter>
      <App />
    </HashRouter>
  );
};

export default Root;